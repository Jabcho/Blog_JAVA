spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    defer-datasource-initialization: true

  datasource:
    url: jdbc:h2:mem:testdb
    username: jabcho
    password: jabcho

  h2:
    console:
      enabled: true

jwt:
  issuer: yunsuu0220@gmail.com
  secret_key: jabcho